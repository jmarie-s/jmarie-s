name: Update badges

on:
  workflow_dispatch:
  schedule:
  #Run at 10am UTC every day
  - cron: "0 11 * * *"

jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: pemtajo/badge-readme@main
